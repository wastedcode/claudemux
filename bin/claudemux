#!/usr/bin/env node
// claudemux CLI entry — the full commander wiring lands in T11.
import process from "node:process";

process.stderr.write("claudemux: CLI not yet wired (T11 pending)\n");
process.exit(1);
